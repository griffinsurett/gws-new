---
// src/components/StaticSections/TestimonialCircleSection.astro
import ItemsTemplate from "@/components/ItemsTemplates/ItemsTemplate.astro";
import CircleImageItem from "@/components/LoopComponents/CircleImageItem.astro";
import Heading from "@/components/Heading";
---

<div class="testimonials flex items-center gap-[var(--spacing-sm)] px-[var(--spacing-md)] md:px-0">
  <!-- clip the overflow so only the “stack” shows -->
    <ItemsTemplate
      collection="testimonials"
      query="getAll"
      ItemComponent={CircleImageItem}
      itemsClass="flex"             
      itemClass="-mr-4 first:ml-0" 
    />

  <div class="flex flex-col items-start gap-[var(--spacing-xs)] ml-[var(--spacing-md)]">
    <Heading tagName="h2" className="text-heading m-0 p-0">100+</Heading>
    <Heading tagName="p" className="text-small m-0 ml-[var(--spacing-sm)]">
      Happy Clients
    </Heading>
  </div>
</div>
